stages:
  - release
  - publish

create_tag:
  image: node:latest
  stage: release
  script:
    - npx semantic-release

test_tag:
  image: ubuntu:latest
  stage: publish
  script:
    - echo ${CI_COMMIT_TAG}
